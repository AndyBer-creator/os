/*
 * Note: this file originally auto-generated by mib2c using
 *        : mib2c.old-api.conf 17851 2009-11-30 16:46:06Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include <common/sys_def.h>
#include <libsal/sal_mib_private.h>
#include <common/sys_bitmap.h>

#include "dhcpSnoopingMib.h"

/* 
 * dhcpSnoopingMib_variables_oid:
 *   this is the top level oid that we want to register under.  This
 *   is essentially a prefix, with the suffix appearing in the
 *   variable below.
 */

oid dhcpSnoopingMib_variables_oid[] = { 1,3,6,1,4,1,27282,1,1,1,5 };

/* 
 * variable4 dhcpSnoopingMib_variables:
 *   this variable defines function callbacks and type return information 
 *   for the dhcpSnoopingMib mib section 
 */

struct variable4 dhcpSnoopingMib_variables[] = {
/*  magic number        , variable type , ro/rw , callback fn  , L, oidsuffix */
#define DHCPSNOOPINGONOFF		1
{DHCPSNOOPINGONOFF,  ASN_INTEGER,  RWRITE,  var_dhcpSnoopingMib, 1,  { 1 }},

#define VLANINDEX		1
{VLANINDEX,  ASN_INTEGER,  RONLY,   var_dhcpSnoopingVlanTable, 3,  { 2 , 1, 1 }},
#define VLANDHCPSNOOPINGONOFF		2
{VLANDHCPSNOOPINGONOFF,  ASN_INTEGER,  RWRITE,  var_dhcpSnoopingVlanTable, 3,  { 2 , 1, 2 }},
#define PORTINDEX		1
{PORTINDEX,  ASN_INTEGER,  RONLY,   var_dhcpsnoopingPortTable, 3,  { 3 , 1, 1 }},
#define PORTTRUSTMODE		2
{PORTTRUSTMODE,  ASN_INTEGER,  RWRITE,  var_dhcpsnoopingPortTable, 3,  { 3 , 1, 2 }},
#define PORTVERIFYMACADDRESS		3
{PORTVERIFYMACADDRESS,  ASN_INTEGER,  RWRITE,  var_dhcpsnoopingPortTable, 3,  { 3 , 1, 3 }},
#define PORTRATELIMIT		4
{PORTRATELIMIT,  ASN_INTEGER,  RWRITE,  var_dhcpsnoopingPortTable, 3,  { 3 , 1, 4 }},
};
/*    (L = length of the oidsuffix) */


/** Initializes the dhcpSnoopingMib module */
void
init_dhcpSnoopingMib(void)
{

    DEBUGMSGTL(("dhcpSnoopingMib", "Initializing\n"));

	sal_private_oid_oem_init(dhcpSnoopingMib_variables_oid);

    /* register ourselves with the agent to handle our mib tree */
    REGISTER_MIB("dhcpSnoopingMib", dhcpSnoopingMib_variables, variable4,
               dhcpSnoopingMib_variables_oid);

    /* place any other initialization junk you need here */
}

/*
 * var_dhcpSnoopingMib():
 *   This function is called every time the agent gets a request for
 *   a scalar variable that might be found within your mib section
 *   registered above.  It is up to you to do the right thing and
 *   return the correct value.
 *     You should also correct the value of "var_len" if necessary.
 *
 *   Please see the documentation for more information about writing
 *   module extensions, and check out the examples in the examples
 *   and mibII directories.
 */
unsigned char *
var_dhcpSnoopingMib(struct variable *vp, 
                oid     *name, 
                size_t  *length, 
                int     exact, 
                size_t  *var_len, 
                WriteMethod **write_method)
{
    /* variables we may use later */
    static long long_ret;
    static u_long ulong_ret;
    static unsigned char string[SPRINT_MAX_LEN];
    static oid objid[MAX_OID_LEN];
    static struct counter64 c64;

    if (header_generic(vp,name,length,exact,var_len,write_method)
                                  == MATCH_FAILED )
    return NULL;

    /* 
   * this is where we do the value assignments for the mib results.
   */
    switch(vp->magic) {
    case DHCPSNOOPINGONOFF:
        *write_method = write_dhcpsnoopingOnOff;
        sal_dhcp_snooping_enable_get(&ulong_ret);
        if(ulong_ret)
        {
			ulong_ret = MIB_ENABLE;
        }
        else
        {
			ulong_ret = MIB_DISABLE;
        }
        return (u_char*) &ulong_ret;
    default:
      ERROR_MSG("");
    }
    return NULL;
}


/*
 * var_dhcpSnoopingVlanTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_dhcpSnoopingMib above.
 */
unsigned char *
var_dhcpSnoopingVlanTable(struct variable *vp,
    	    oid     *name,
    	    size_t  *length,
    	    int     exact,
    	    size_t  *var_len,
    	    WriteMethod **write_method)
{
    /* variables we may use later */
    static long long_ret;
    static u_long ulong_ret;
    static unsigned char string[SPRINT_MAX_LEN];
    static oid objid[MAX_OID_LEN];
    static struct counter64 c64;
    unsigned long	vlanindex;
    sys_vlanmask_t  vlanMask;
    
    /* 
   * This assumes that the table is a 'simple' table.
   *	See the implementation documentation for the meaning of this.
   *	You will need to provide the correct value for the TABLE_SIZE parameter
   *
   * If this table does not meet the requirements for a simple table,
   *	you will need to provide the replacement code yourself.
   *	Mib2c is not smart enough to write this for you.
   *    Again, see the implementation documentation for what is required.
   */
    if (header_simple_table(vp,name,length,exact,var_len,write_method, BOND_VLAN_MAX+1)
                                                == MATCH_FAILED )
    return NULL;
	vlanindex = vp->namelen >= *length ? 0 : name[vp->namelen];
	if (exact) /* GET packet for one instance */
	{
		if (*length != 15 )
			return NULL;
	}
	else /* Get-Next packet */
	{
		VLANMASK_CLEAR_ALL(vlanMask);
		VLANMASK_SET_VID(vlanMask, vlanindex);
		if (-1 == header_vlan(&vlanindex, vp, name, length, vlanMask))
			return NULL;
	}

	if (IS_VID_INVALID(vlanindex))
		return NULL;

	*var_len = sizeof(long);
    /* 
   * this is where we do the value assignments for the mib results.
   */
    switch(vp->magic) {
    case VLANINDEX:
        ulong_ret = vlanindex;	/* XXX */
        return (u_char*) &ulong_ret;
    case VLANDHCPSNOOPINGONOFF:
        *write_method = write_vlandhcpsnoopingOnOff;
        sal_dhcp_snooping_vlan_enable_get(vlanindex, &ulong_ret);
        if(ulong_ret)
        {
			ulong_ret = MIB_ENABLE;
        }
        else
        {
			ulong_ret = MIB_DISABLE;
        }
        return (u_char*) &ulong_ret;
    default:
      ERROR_MSG("");
    }
    return NULL;
}
/*
 * var_dhcpsnoopingPortTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_dhcpSnoopingMib above.
 */
unsigned char *
var_dhcpsnoopingPortTable(struct variable *vp,
    	    oid     *name,
    	    size_t  *length,
    	    int     exact,
    	    size_t  *var_len,
    	    WriteMethod **write_method)
{
    /* variables we may use later */
    static long long_ret;
    static u_long ulong_ret;
    static unsigned char string[SPRINT_MAX_LEN];
    static oid objid[MAX_OID_LEN];
    static struct counter64 c64;
	unsigned long	index;

    /* 
   * This assumes that the table is a 'simple' table.
   *	See the implementation documentation for the meaning of this.
   *	You will need to provide the correct value for the TABLE_SIZE parameter
   *
   * If this table does not meet the requirements for a simple table,
   *	you will need to provide the replacement code yourself.
   *	Mib2c is not smart enough to write this for you.
   *    Again, see the implementation documentation for what is required.
   */
    if (header_simple_table(vp,name,length,exact,var_len,write_method, LP2UP(LOGIC_PORT_END) + 1)
                                                == MATCH_FAILED )
    return NULL;
    
	index = vp->namelen >= *length ? 0 : UP2LP(name[vp->namelen]);
		
	if (exact) /* GET packet for one instance */
	{
		if (*length != 15 )
			return NULL;
	}
	else /* Get-Next packet */
	{
		if (-1 == header_logicPort(&index, vp, name, length))
			return NULL;
	}

	if (IS_LOGIC_PORT_INVALID(index))
		return NULL;

	*var_len = sizeof(long);

    /* 
   * this is where we do the value assignments for the mib results.
   */
    switch(vp->magic) {
    case PORTINDEX:
        ulong_ret = LP2UP(index);
        return (u_char*) &ulong_ret;
    case PORTTRUSTMODE:
        *write_method = write_portTrustMode;
        sal_dhcp_snooping_port_trust_get(index, &ulong_ret);
        if(ulong_ret)
        {
			ulong_ret = MIB_ENABLE;
        }
        else
        {
			ulong_ret = MIB_DISABLE;
        }
        return (u_char*) &ulong_ret;
    case PORTVERIFYMACADDRESS:
        *write_method = write_portVerifyMacAddress;
        sal_dhcp_snooping_portChaddrChk_get(index, &ulong_ret);
        if(ulong_ret)
        {
			ulong_ret = MIB_ENABLE;
        }
        else
        {
			ulong_ret = MIB_DISABLE;
        }
        return (u_char*) &ulong_ret;
    case PORTRATELIMIT:
        *write_method = write_portRateLimit;
        sal_dhcp_snooping_rateLimit_get(index, &ulong_ret);
        if(ulong_ret == BOND_RATE_UNLIMITED)
        	ulong_ret = 0;
        return (u_char*) &ulong_ret;
    default:
      ERROR_MSG("");
    }
    return NULL;
}



int
write_dhcpsnoopingOnOff(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to dhcpSnoopingMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to dhcpSnoopingMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if(value == MIB_ENABLE)
          {
			value = TRUE;
          }
          else 
          {
			value = FALSE;
          }
		  sal_dhcp_snooping_enable_set(value);
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}

int
write_vlandhcpsnoopingOnOff(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;
    int vlanindex;
	vlanindex = name[name_len-1];

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to dhcpSnoopingMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to dhcpSnoopingMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if(value == MIB_ENABLE)
          {
			value = TRUE;
          }
          else 
          {
			value = FALSE;
          }
		  sal_dhcp_snooping_vlan_enable_set(vlanindex, value);
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_portTrustMode(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;
    ulong index;
    
	index = UP2LP(name[name_len-1]);

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to dhcpSnoopingMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to dhcpSnoopingMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if(value == MIB_ENABLE)
          {
			 value = TRUE;
          }
          else 
          {
			 value = FALSE;
          }
		  sal_dhcp_snooping_port_trust_set(index, value);
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_portVerifyMacAddress(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;
    ulong index;
    
	index = UP2LP(name[name_len-1]);

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to dhcpSnoopingMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to dhcpSnoopingMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if(value == MIB_ENABLE)
          {
			 value = TRUE;
          }
          else 
          {
			 value = FALSE;
          }
		  sal_dhcp_snooping_portChaddrChk_set(index, value);

          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_portRateLimit(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;
    ulong index;
    
	index = UP2LP(name[name_len-1]);

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to dhcpSnoopingMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to dhcpSnoopingMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if(0 <= value && value <= BOND_RATE_UNLIMITED)
		  	  sal_dhcp_snooping_rateLimit_set(index, value);
		  else 
		  	  return SNMP_ERR_WRONGVALUE;
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
