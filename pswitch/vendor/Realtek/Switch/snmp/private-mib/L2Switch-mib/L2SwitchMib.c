/*
 * Note: this file originally auto-generated by mib2c using
 *        : mib2c.old-api.conf 17851 2009-11-30 16:46:06Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "L2SwitchMib.h"
#include <common/sys_def.h>
#include <libsal/sal_mib_private.h>

/* 
 * L2SwitchMib_variables_oid:
 *   this is the top level oid that we want to register under.  This
 *   is essentially a prefix, with the suffix appearing in the
 *   variable below.
 */

oid L2SwitchMib_variables_oid[] = { 1,3,6,1,4,1,27282,1,1,1,11 };

sys_l2_entry_t gL2_entry;

/* 
 * variable4 L2SwitchMib_variables:
 *   this variable defines function callbacks and type return information 
 *   for the L2SwitchMib mib section 
 */

struct variable4 L2SwitchMib_variables[] = {
/*  magic number        , variable type , ro/rw , callback fn  , L, oidsuffix */

#define FILTERMACADDR		1
{FILTERMACADDR,  ASN_OCTET_STR,  RWRITE,  var_L2macAddrFilterTable, 4,  { 1,1 , 1, 1 }},
#define VLANID		2
{VLANID,  ASN_INTEGER,  RWRITE,  var_L2macAddrFilterTable, 4,  { 1,1 , 1, 2 }},
#define MACROWSTATUS		3
{MACROWSTATUS,  ASN_INTEGER,  RWRITE,  var_L2macAddrFilterTable, 4,  { 1,1 , 1, 3 }},
};
/*    (L = length of the oidsuffix) */


/** Initializes the L2SwitchMib module */
void
init_L2SwitchMib(void)
{
    DEBUGMSGTL(("L2SwitchMib", "Initializing\n"));

	sal_private_oid_oem_init(L2SwitchMib_variables_oid);
	memset(&gL2_entry, 0, sizeof(sys_l2_entry_t));

    /* register ourselves with the agent to handle our mib tree */
    REGISTER_MIB("L2SwitchMib", L2SwitchMib_variables, variable4,
               L2SwitchMib_variables_oid);	
    /* place any other initialization junk you need here */
}

/*
 * var_L2SwitchMib():
 *   This function is called every time the agent gets a request for
 *   a scalar variable that might be found within your mib section
 *   registered above.  It is up to you to do the right thing and
 *   return the correct value.
 *     You should also correct the value of "var_len" if necessary.
 *
 *   Please see the documentation for more information about writing
 *   module extensions, and check out the examples in the examples
 *   and mibII directories.
 */
unsigned char *
var_L2SwitchMib(struct variable *vp, 
                oid     *name, 
                size_t  *length, 
                int     exact, 
                size_t  *var_len, 
                WriteMethod **write_method)
{
    /* variables we may use later */
    static long long_ret;
    static u_long ulong_ret;
    static unsigned char string[SPRINT_MAX_LEN];
    static oid objid[MAX_OID_LEN];
    static struct counter64 c64;

    if (header_generic(vp,name,length,exact,var_len,write_method)
                                  == MATCH_FAILED )
    return NULL;

    /* 
   * this is where we do the value assignments for the mib results.
   */
    switch(vp->magic) {
    default:
      ERROR_MSG("");
    }
    return NULL;
}


/*
 * var_L2macAddrFilterTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_L2SwitchMib above.
 */
unsigned char *
var_L2macAddrFilterTable(struct variable *vp,
    	    oid     *name,
    	    size_t  *length,
    	    int     exact,
    	    size_t  *var_len,
    	    WriteMethod **write_method)
{
    /* variables we may use later */
    static long long_ret;
    static u_long ulong_ret;
    static unsigned char string[SPRINT_MAX_LEN];
    static oid objid[MAX_OID_LEN];
    static struct counter64 c64;
    static char macstring[32];
    u_long L2index = 0;
	sys_l2_entry_t *pCurr = NULL;

    /* 
   * This assumes that the table is a 'simple' table.
   *	See the implementation documentation for the meaning of this.
   *	You will need to provide the correct value for the TABLE_SIZE parameter
   *
   * If this table does not meet the requirements for a simple table,
   *	you will need to provide the replacement code yourself.
   *	Mib2c is not smart enough to write this for you.
   *    Again, see the implementation documentation for what is required.
   */
    if (header_simple_table(vp,name,length,exact,var_len,write_method, CAPA_L2_FILTER_MAC_NUM+1)
                                                == MATCH_FAILED )
    return NULL;
   
	L2index = vp->namelen >= *length ? 0 : name[vp->namelen];
	if(L2index < 1 || L2index > CAPA_L2_FILTER_MAC_NUM)
	{
		return NULL;
	}

    /* 
   * this is where we do the value assignments for the mib results.
   */
    switch(vp->magic) {
    case FILTERMACADDR:
        *write_method = write_FilterMacAddr;
        break;
    case VLANID:
        *write_method = write_VlanId;
		break;
    case MACROWSTATUS:
        *write_method = write_MacRowStatus;
		break;
    default:
       *write_method = NULL;
    }
    L2index -= 1;
	if (exact){
		
		while(pCurr == NULL)
		{
			pCurr = sal_l2_blacklistMacByIdx_ret(L2index);
			if (NULL != pCurr)
				break;
			L2index ++;
			if(CAPA_L2_FILTER_MAC_NUM == L2index)
				return NULL;
		}
	} else{
		while(pCurr == NULL)
		{
			pCurr = sal_l2_blacklistMacByIdx_ret(L2index);
			if (NULL != pCurr)
				break;
			L2index ++;
			if(CAPA_L2_FILTER_MAC_NUM == L2index)
				return NULL;
		}
	}

    
    switch(vp->magic) {
    case FILTERMACADDR:
        *var_len = CAPA_MAC_BYTE_NUM;
        return (u_char*)pCurr->mac.octet;
    case VLANID:
        long_ret = pCurr->vid;	/* XXX */
        return (u_char*) &long_ret;
    case MACROWSTATUS:
        long_ret = 3;	/* XXX */
        return (u_char*) &long_ret;
    default:
      ERROR_MSG("");
    }
    return NULL;
}


int
write_FilterMacAddr(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    char value;
    int size;
	sys_l2_entry_t entry;
	memset(&entry, 0, sizeof(sys_l2_entry_t));

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_OCTET_STR) {
              fprintf(stderr, "write to L2SwitchMib not ASN_OCTET_STR\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > 18 || var_val_len < 17) {
              fprintf(stderr,"write to L2SwitchMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
		  sys_util_str2Mac(var_val, entry.mac.octet);
		  if(0x1 & entry.mac.octet[0])
		  	return SNMP_ERR_WRONGVALUE;
		  memcpy(&gL2_entry.mac, &entry.mac, sizeof(sys_mac_t));
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_VlanId(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    u_long value;
    int size;

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to L2SwitchMib not ASN_UNSIGNED\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(u_long)) {
              fprintf(stderr,"write to L2SwitchMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (u_long *) var_val;
		  if( 1 > value || value > 4095 )
		  {
			return SNMP_ERR_WRONGVALUE;
		  }
		  gL2_entry.vid = value;
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_MacRowStatus(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;
	sys_mac_t mac;
	memset(&mac, 0, sizeof(sys_mac_t));

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to L2SwitchMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to L2SwitchMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
		  if(value == 4)/*add*/
		  {
		  	if((0 != osal_memcmp(&mac, &gL2_entry.mac, CAPA_MAC_BYTE_NUM) && gL2_entry.vid != 0))
		  	{
				if(sal_l2_blacklistMac_add(&gL2_entry)!= SNMP_ERR_NOERROR)
					return SNMP_ERR_UNDOFAILED;
			}	
		  }
		  else if(value == 6)
		  {
			if((0 != osal_memcmp(&mac, &gL2_entry.mac, CAPA_MAC_BYTE_NUM) && gL2_entry.vid != 0))
		  	{
				if(sal_l2_mac_search(&gL2_entry)!= SNMP_ERR_NOERROR)
					return SNMP_ERR_UNDOFAILED;
				if (SYS_L2_TYPE_BLACKLIST == gL2_entry.type)
				{
        			if(sal_l2_blacklistMac_del(&gL2_entry) != SNMP_ERR_NOERROR)
        				return SNMP_ERR_UNDOFAILED;
        		}
        		else 
        		{
					return SNMP_ERR_WRONGVALUE;
        		}
			}
		  }
		  else 
		  {
			  return SNMP_ERR_WRONGVALUE;
		  }
		  memset(&gL2_entry, 0, sizeof(sys_l2_entry_t));
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
