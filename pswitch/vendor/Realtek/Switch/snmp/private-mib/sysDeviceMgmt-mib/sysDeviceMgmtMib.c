/*
 * Note: this file originally auto-generated by mib2c using
 *        : mib2c.old-api.conf 17851 2009-11-30 16:46:06Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "sysDeviceMgmtMib.h"
#include <common/sys_def.h>
#include <libsal/sal_util.h>
#include <libsal/sal_mib_private.h>

/* 
 * sysDeviceMgmtMib_variables_oid:
 *   this is the top level oid that we want to register under.  This
 *   is essentially a prefix, with the suffix appearing in the
 *   variable below.
 */

oid sysDeviceMgmtMib_variables_oid[] = { 1,3,6,1,4,1,27282,1,1,1,6 };

/* 
 * variable4 sysDeviceMgmtMib_variables:
 *   this variable defines function callbacks and type return information 
 *   for the sysDeviceMgmtMib mib section 
 */

struct variable4 sysDeviceMgmtMib_variables[] = {
/*  magic number        , variable type , ro/rw , callback fn  , L, oidsuffix */
#define SYSTEMREBOOT		1
{SYSTEMREBOOT,  ASN_INTEGER,  RWRITE,  var_sysDeviceMgmtMib, 2,  { 2,1 }},
#define TFTPSERVER		2
{TFTPSERVER,  ASN_IPADDRESS,  RWRITE,  var_sysDeviceMgmtMib, 2,  { 3,1 }},
#define FILENAME		3
{FILENAME,  ASN_OCTET_STR,  RWRITE,  var_sysDeviceMgmtMib, 2,  { 3,2 }},
#define IMGUPGRADE		4
{IMGUPGRADE,  ASN_INTEGER,  RWRITE,  var_sysDeviceMgmtMib, 2,  { 3,3 }},
#define DOWNLOAD		5
{DOWNLOAD,  ASN_INTEGER,  RWRITE,  var_sysDeviceMgmtMib, 2,  { 3,4 }},
#define UPLOAD		6
{UPLOAD,  ASN_INTEGER,  RWRITE,  var_sysDeviceMgmtMib, 2,  { 3,5 }},
#define SYSTEMBUILDRUNCONFIG		7
{SYSTEMBUILDRUNCONFIG,  ASN_INTEGER,  RWRITE,  var_sysDeviceMgmtMib, 2,  { 4,1 }},
#define SYSTEMBUILDCLONECONFIG		8
{SYSTEMBUILDCLONECONFIG,  ASN_INTEGER,  RWRITE,  var_sysDeviceMgmtMib, 2,  { 4,2 }},
#define SYSTEMRUNTOFLASHCONFIG		9
{SYSTEMRUNTOFLASHCONFIG,  ASN_INTEGER,  RWRITE,  var_sysDeviceMgmtMib, 2,  { 4,3 }},
#define SYSTEMFLASHTORUNCONFIG		10
{SYSTEMFLASHTORUNCONFIG,  ASN_INTEGER,  RWRITE,  var_sysDeviceMgmtMib, 2,  { 4,4 }},
#define SYSTEMCLONETORUNCONFIG		11
{SYSTEMCLONETORUNCONFIG,  ASN_INTEGER,  RWRITE,  var_sysDeviceMgmtMib, 2,  { 4,5 }},

#define IPIFINDEX		1
{IPIFINDEX,  ASN_INTEGER,  RONLY,   var_deviceipTable, 3,  { 1 , 1, 1 }},
#define IPADDRESS		2
{IPADDRESS,  ASN_IPADDRESS,  RONLY,   var_deviceipTable, 3,  { 1 , 1, 2 }},
#define IPNETMASK		3
{IPNETMASK,  ASN_IPADDRESS,  RONLY,   var_deviceipTable, 3,  { 1 , 1, 3 }},
};
/*    (L = length of the oidsuffix) */


/** Initializes the sysDeviceMgmtMib module */
void
init_sysDeviceMgmtMib(void)
{

    DEBUGMSGTL(("sysDeviceMgmtMib", "Initializing\n"));

	sal_private_oid_oem_init(sysDeviceMgmtMib_variables_oid);

    /* register ourselves with the agent to handle our mib tree */
    REGISTER_MIB("sysDeviceMgmtMib", sysDeviceMgmtMib_variables, variable4,
               sysDeviceMgmtMib_variables_oid);

    /* place any other initialization junk you need here */
}

/*
 * var_sysDeviceMgmtMib():
 *   This function is called every time the agent gets a request for
 *   a scalar variable that might be found within your mib section
 *   registered above.  It is up to you to do the right thing and
 *   return the correct value.
 *     You should also correct the value of "var_len" if necessary.
 *
 *   Please see the documentation for more information about writing
 *   module extensions, and check out the examples in the examples
 *   and mibII directories.
 */
unsigned char *
var_sysDeviceMgmtMib(struct variable *vp, 
                oid     *name, 
                size_t  *length, 
                int     exact, 
                size_t  *var_len, 
                WriteMethod **write_method)
{
    /* variables we may use later */
    static long long_ret;
    static u_long ulong_ret;
    static unsigned char string[SPRINT_MAX_LEN];
    static oid objid[MAX_OID_LEN];
    static struct counter64 c64;
    char ipStr[CAPA_IPV4_STR_LEN];

    if (header_generic(vp,name,length,exact,var_len,write_method)
                                  == MATCH_FAILED )
    return NULL;

    /* 
   * this is where we do the value assignments for the mib results.
   */
    switch(vp->magic) {
    case SYSTEMREBOOT:
        *write_method = write_systemReboot;
        ulong_ret = 0;	/* XXX */
        return (u_char*) &ulong_ret;
    case TFTPSERVER:
        *write_method = write_tftpServer;
        sal_mib_file_upgrade_get(TFTPSERVER, ipStr);
        STR2IPV4(ipStr, &ulong_ret);
        return (u_char*) &ulong_ret;
    case FILENAME:
        *write_method = write_FileName;
        sal_mib_file_upgrade_get(FILENAME, string);
        *var_len = 128;
        return (u_char*) &string;
    case IMGUPGRADE:
        *write_method = write_imgupgrade;
        sal_mib_file_upgrade_get(IMGUPGRADE, &ulong_ret);
        return (u_char*) &ulong_ret;
    case DOWNLOAD:
        *write_method = write_download;
        sal_mib_config_file_status_get(DOWNLOAD, &ulong_ret);
        return (u_char*) &ulong_ret;
    case UPLOAD:
        *write_method = write_upload;
        sal_mib_config_file_status_get(UPLOAD, &ulong_ret);
        return (u_char*) &ulong_ret;
    case SYSTEMBUILDRUNCONFIG:
        *write_method = write_systemBuildRunConfig;
        sal_mib_config_file_status_get(SYSTEMBUILDRUNCONFIG, &ulong_ret);
        return (u_char*) &ulong_ret;
    case SYSTEMBUILDCLONECONFIG:
        *write_method = write_systemBuildCloneConfig;
        sal_mib_config_file_status_get(SYSTEMBUILDCLONECONFIG, &ulong_ret);
        return (u_char*) &ulong_ret;
    case SYSTEMRUNTOFLASHCONFIG:
        *write_method = write_systemRuntoFlashConfig;
        sal_mib_config_file_status_get(SYSTEMRUNTOFLASHCONFIG, &ulong_ret);
        return (u_char*) &ulong_ret;
    case SYSTEMFLASHTORUNCONFIG:
        *write_method = write_systemFlashToRunConfig;
        sal_mib_config_file_status_get(SYSTEMFLASHTORUNCONFIG, &ulong_ret);
        return (u_char*) &ulong_ret;
    case SYSTEMCLONETORUNCONFIG:
        *write_method = write_systemCloneToRunConfig;
        sal_mib_config_file_status_get(SYSTEMCLONETORUNCONFIG, &ulong_ret);
        return (u_char*) &ulong_ret;
    default:
      ERROR_MSG("");
    }
    return NULL;
}


/*
 * var_deviceipTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_sysDeviceMgmtMib above.
 */
unsigned char *
var_deviceipTable(struct variable *vp,
    	    oid     *name,
    	    size_t  *length,
    	    int     exact,
    	    size_t  *var_len,
    	    WriteMethod **write_method)
{
    /* variables we may use later */
    static long long_ret;
    static u_long ulong_ret;
    static unsigned char string[SPRINT_MAX_LEN];
    static oid objid[MAX_OID_LEN];
    static struct counter64 c64;

    /* 
   * This assumes that the table is a 'simple' table.
   *	See the implementation documentation for the meaning of this.
   *	You will need to provide the correct value for the TABLE_SIZE parameter
   *
   * If this table does not meet the requirements for a simple table,
   *	you will need to provide the replacement code yourself.
   *	Mib2c is not smart enough to write this for you.
   *    Again, see the implementation documentation for what is required.
   */
    if (header_simple_table(vp,name,length,exact,var_len,write_method, 1)
                                                == MATCH_FAILED )
    return NULL;
    
    /* 
   * this is where we do the value assignments for the mib results.
   */
    switch(vp->magic) {
    case IPIFINDEX:
        ulong_ret = 1;	/* XXX */
        return (u_char*) &ulong_ret;
    case IPADDRESS:
    	sal_mib_device_ip_get(IPADDRESS, &ulong_ret);
        return (u_char*) &ulong_ret;
    case IPNETMASK:
        sal_mib_device_ip_get(IPNETMASK, &ulong_ret);
        return (u_char*) &ulong_ret;
    default:
      ERROR_MSG("");
    }
    return NULL;
}



int
write_systemReboot(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to sysDeviceMgmtMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to sysDeviceMgmtMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if(value == 1)
          {
          	  system("handler -f reboot 1> /dev/null 2> /dev/null &");
		      //sal_sys_reboot();
		  }
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}


int
write_tftpServer(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    in_addr_t value;
    int size;
	char ipStr[CAPA_IPV4_STR_LEN];

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_IPADDRESS) {
              fprintf(stderr, "write to sysDeviceMgmtMib not ASN_IPADDRESS\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(in_addr_t)) {
              fprintf(stderr,"write to sysDeviceMgmtMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (in_addr_t *) var_val;
          sys_util_ip2Str(value, ipStr);
		  sal_mib_file_upgrade_set(TFTPSERVER, ipStr);
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}


int
write_FileName(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    char value[128];
    int size;

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_OCTET_STR) {
              fprintf(stderr, "write to sysDeviceMgmtMib not ASN_OCTET_STR\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(value)) {
              fprintf(stderr,"write to sysDeviceMgmtMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          //value = * (char *) var_val;
          osal_memset(value, 0, sizeof(value));
          osal_memcpy(value, var_val, size);
		  sal_mib_file_upgrade_set(FILENAME, &value);

          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}


int
write_imgupgrade(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to sysDeviceMgmtMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to sysDeviceMgmtMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if(value == 1)
		  	sal_mib_file_upgrade_set(IMGUPGRADE, (char *)&value);

          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}


int
write_download(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to sysDeviceMgmtMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to sysDeviceMgmtMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if(value > 3 && value < 7)
          {
			 sal_mib_file_upgrade_set(DOWNLOAD, &value);
          }
          else
          	 return SNMP_ERR_WRONGVALUE;

          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}


int
write_upload(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to sysDeviceMgmtMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to sysDeviceMgmtMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if(value > 3 && value < 7)
          {
			 sal_mib_file_upgrade_set(UPLOAD, &value);
          }
          else
          	 return SNMP_ERR_WRONGVALUE;

          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}


int
write_systemBuildRunConfig(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to sysDeviceMgmtMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to sysDeviceMgmtMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if(value == 1)
          {
		  	system("handler -f runconfig 1> /dev/null 2> /dev/null &");
		  	sal_mib_build_config_file_set(SYSTEMBUILDRUNCONFIG);
		  }
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}


int
write_systemBuildCloneConfig(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to sysDeviceMgmtMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to sysDeviceMgmtMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if(value == 1)
          {
		  	system("handler -f cloneconfig 1> /dev/null 2> /dev/null &");
		  	sal_mib_build_config_file_set(SYSTEMBUILDCLONECONFIG);
		  }

          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}


int
write_systemRuntoFlashConfig(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to sysDeviceMgmtMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to sysDeviceMgmtMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
		  if(value == 1)
          {
		  	system("handler -f mibsaverunconfig 1> /dev/null 2> /dev/null &");	
		  	sal_mib_file_config_save_flash_set();
		  }

          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}


int
write_systemFlashToRunConfig(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to sysDeviceMgmtMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to sysDeviceMgmtMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
		  if(value == 1)
          {
		  	system("handler -f mibstartuptorunconfig 1> /dev/null 2> /dev/null &");	
		  	sal_mib_file_startup_to_run_set();
		  }

          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}


int
write_systemCloneToRunConfig(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to sysDeviceMgmtMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to sysDeviceMgmtMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
		  if(value == 1)
          {
		  	system("handler -f mibclonetorunconfig 1> /dev/null 2> /dev/null &");	
		  	sal_mib_file_clone_to_run_set();
		  }
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}

