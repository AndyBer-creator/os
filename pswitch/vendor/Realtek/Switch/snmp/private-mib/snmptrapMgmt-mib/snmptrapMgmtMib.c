/*
 * Note: this file originally auto-generated by mib2c using
 *        : mib2c.old-api.conf 17851 2009-11-30 16:46:06Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include <common/sys_def.h>
#include <libsal/sal_util.h>
#include <libsal/sal_mib_private.h>

#include "snmptrapMgmtMib.h"

/* 
 * snmptrapMgmtMib_variables_oid:
 *   this is the top level oid that we want to register under.  This
 *   is essentially a prefix, with the suffix appearing in the
 *   variable below.
 */

oid snmptrapMgmtMib_variables_oid[] = { 1,3,6,1,4,1,27282,1,1,1,10 };

/* 
 * variable4 snmptrapMgmtMib_variables:
 *   this variable defines function callbacks and type return information 
 *   for the snmptrapMgmtMib mib section 
 */

struct variable4 snmptrapMgmtMib_variables[] = {
/*  magic number        , variable type , ro/rw , callback fn  , L, oidsuffix */

#define HOSTINDEX		1
{HOSTINDEX,  ASN_INTEGER,  RONLY,   var_snmpvTrapConfigTable, 3,  { 1 , 1, 1 }},
#define HOSTIPADDR		2
{HOSTIPADDR,  ASN_OCTET_STR,  RWRITE,  var_snmpvTrapConfigTable, 3,  { 1 , 1, 2 }},
#define VERSION		3
{VERSION,  ASN_INTEGER,  RWRITE,  var_snmpvTrapConfigTable, 3,  { 1 , 1, 3 }},
#define COMMUNITYNAME		4
{COMMUNITYNAME,  ASN_OCTET_STR,  RWRITE,  var_snmpvTrapConfigTable, 3,  { 1 , 1, 4 }},
#define V3SECURITYLEVEL		5
{V3SECURITYLEVEL,  ASN_INTEGER,  RWRITE,  var_snmpvTrapConfigTable, 3,  { 1 , 1, 5 }},
#define UDPPORT		6
{UDPPORT,  ASN_INTEGER,  RWRITE,  var_snmpvTrapConfigTable, 3,  { 1 , 1, 6 }},
#define TRAPSTATUS		7
{TRAPSTATUS,  ASN_INTEGER,  RWRITE,  var_snmpvTrapConfigTable, 3,  { 1 , 1, 7 }},
};
/*    (L = length of the oidsuffix) */


/** Initializes the snmptrapMgmtMib module */
void
init_snmptrapMgmtMib(void)
{

    DEBUGMSGTL(("snmptrapMgmtMib", "Initializing\n"));

	sal_private_oid_oem_init(snmptrapMgmtMib_variables_oid);

    /* register ourselves with the agent to handle our mib tree */
    REGISTER_MIB("snmptrapMgmtMib", snmptrapMgmtMib_variables, variable4,
               snmptrapMgmtMib_variables_oid);

    /* place any other initialization junk you need here */
}

/*
 * var_snmptrapMgmtMib():
 *   This function is called every time the agent gets a request for
 *   a scalar variable that might be found within your mib section
 *   registered above.  It is up to you to do the right thing and
 *   return the correct value.
 *     You should also correct the value of "var_len" if necessary.
 *
 *   Please see the documentation for more information about writing
 *   module extensions, and check out the examples in the examples
 *   and mibII directories.
 */
unsigned char *
var_snmptrapMgmtMib(struct variable *vp, 
                oid     *name, 
                size_t  *length, 
                int     exact, 
                size_t  *var_len, 
                WriteMethod **write_method)
{
    /* variables we may use later */
    static long long_ret;
    static u_long ulong_ret;
    static unsigned char string[SPRINT_MAX_LEN];
    static oid objid[MAX_OID_LEN];
    static struct counter64 c64;

    if (header_generic(vp,name,length,exact,var_len,write_method)
                                  == MATCH_FAILED )
    return NULL;

    /* 
   * this is where we do the value assignments for the mib results.
   */
    switch(vp->magic) {
    default:
      ERROR_MSG("");
    }
    return NULL;
}


/*
 * var_snmpvTrapConfigTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_snmptrapMgmtMib above.
 */
unsigned char *
var_snmpvTrapConfigTable(struct variable *vp,
    	    oid     *name,
    	    size_t  *length,
    	    int     exact,
    	    size_t  *var_len,
    	    WriteMethod **write_method)
{
    /* variables we may use later */
    static long long_ret;
    static u_long ulong_ret;
    static unsigned char string[CAPA_HOSTNAME_LEN+1];
    static oid objid[MAX_OID_LEN];
    static struct counter64 c64;
	unsigned long	index;
	char ipStr[CAPA_IPV4_STR_LEN];

    /* 
   * This assumes that the table is a 'simple' table.
   *	See the implementation documentation for the meaning of this.
   *	You will need to provide the correct value for the TABLE_SIZE parameter
   *
   * If this table does not meet the requirements for a simple table,
   *	you will need to provide the replacement code yourself.
   *	Mib2c is not smart enough to write this for you.
   *    Again, see the implementation documentation for what is required.
   */
    if (header_simple_table(vp,name,length,exact,var_len,write_method, 8)
                                                == MATCH_FAILED )
    return NULL;

    index = vp->namelen >= *length ? 0 : name[vp->namelen];

    /* 
   * this is where we do the value assignments for the mib results.
   */
    switch(vp->magic) {
    case HOSTINDEX:
        ulong_ret = index;	/* XXX */
        return (u_char*) &ulong_ret;
    case HOSTIPADDR:
        *write_method = write_hostIpAddr;
        sal_mib_snmp_trap_get(HOSTIPADDR, index-1, string);
        //STR2IPV4(ipStr, &ulong_ret);
        *var_len = strlen(string);
        return (u_char*) &string;
    case VERSION:
        *write_method = write_version;
        sal_mib_snmp_trap_get(VERSION, index-1, &ulong_ret);
        return (u_char*) &ulong_ret;
    case COMMUNITYNAME:
        *write_method = write_CommunityName;
        sal_mib_snmp_trap_get(COMMUNITYNAME, index-1, &string);
        *var_len = strlen(string);
        return (u_char*) &string;
    case V3SECURITYLEVEL:
        *write_method = write_v3Securitylevel;
        sal_mib_snmp_trap_get(V3SECURITYLEVEL, index-1, &ulong_ret);
        return (u_char*) &ulong_ret;
    case UDPPORT:
        *write_method = write_udpPort;
        sal_mib_snmp_trap_get(UDPPORT, index-1, &ulong_ret);
        return (u_char*) &ulong_ret;
    case TRAPSTATUS:
        *write_method = write_trapStatus;
        sal_mib_snmp_trap_get(TRAPSTATUS, index-1, &ulong_ret);
        return (u_char*) &ulong_ret;
    default:
      ERROR_MSG("");
    }
    return NULL;
}


int
write_hostIpAddr(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    char value[CAPA_HOSTNAME_LEN+1];
    int size;
    int index = 0;
    int rc = 0;
    
	index = name[name_len-1];

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_OCTET_STR) {
              fprintf(stderr, "write to snmptrapMgmtMib not ASN_OCTET_STR\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(value)) {
              fprintf(stderr,"write to snmptrapMgmtMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          //value = * (char *) var_val;
          osal_memset(value, 0, sizeof(value));
          osal_memcpy(value, var_val, size);
		  rc = sal_mib_snmp_trap_set(HOSTIPADDR, index-1, value);
		  if(rc != SYS_ERR_OK)
		  {
			 return SNMP_ERR_WRONGVALUE;
		  }
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_version(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;
    int index = 0;
    
	index = name[name_len-1];

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to snmptrapMgmtMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to snmptrapMgmtMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if( 0 > value && value > 4)
          	  return SNMP_ERR_WRONGVALUE;
          	  
		  sal_mib_snmp_trap_set(VERSION, index-1, &value);
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_CommunityName(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    char value[CAPA_SNMP_USER_NAME_LEN+1];
    int size;
    int index = 0;
    int rc=  0;
    
	index = name[name_len-1];

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_OCTET_STR) {
              fprintf(stderr, "write to snmptrapMgmtMib not ASN_OCTET_STR\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(value)) {
              fprintf(stderr,"write to snmptrapMgmtMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          osal_memset(value, 0, sizeof(value));
          osal_memcpy(value, var_val, size);
		  rc = sal_mib_snmp_trap_set(COMMUNITYNAME, index-1, &value);
		  if(rc != SYS_ERR_OK)
		  {
			 return SNMP_ERR_WRONGVALUE;
		  }

          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_v3Securitylevel(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;
    int index = 0;
    
	index = name[name_len-1];

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to snmptrapMgmtMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to snmptrapMgmtMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if( 0 > value && value > 4)
          	  return SNMP_ERR_WRONGVALUE;
          	  
		  sal_mib_snmp_trap_set(V3SECURITYLEVEL, index-1, &value);
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_udpPort(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;
    int index = 0;
    
	index = name[name_len-1];

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to snmptrapMgmtMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to snmptrapMgmtMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
		  sal_mib_snmp_trap_set(UDPPORT, index-1, &value);

          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_trapStatus(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;
    int index = 0;
    int rc = 0;
    
	index = name[name_len-1];

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to snmptrapMgmtMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to snmptrapMgmtMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if(value < 0 && value > 2)
          	 return SNMP_ERR_WRONGVALUE;
		  rc = sal_mib_snmp_trap_set(TRAPSTATUS, index-1, &value);
		  if(rc != 0)
		  	 return SNMP_ERR_WRONGVALUE;

          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
