/*
 * Note: this file originally auto-generated by mib2c using
 *        : mib2c.old-api.conf 17851 2009-11-30 16:46:06Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include <common/sys_def.h>
#include <libsal/sal_mib_private.h>

#include "sysPortCtrlMib.h"

/* 
 * sysPortCtrlMib_variables_oid:
 *   this is the top level oid that we want to register under.  This
 *   is essentially a prefix, with the suffix appearing in the
 *   variable below.
 */

oid sysPortCtrlMib_variables_oid[] = { 1,3,6,1,4,1,27282,1,1,1,2 };

/* 
 * variable4 sysPortCtrlMib_variables:
 *   this variable defines function callbacks and type return information 
 *   for the sysPortCtrlMib mib section 
 */

struct variable4 sysPortCtrlMib_variables[] = {
/*  magic number        , variable type , ro/rw , callback fn  , L, oidsuffix */

#define PORTINDEX		1
{PORTINDEX,  ASN_INTEGER,  RONLY,   var_portAttributeTable, 3,  { 1 , 1, 1 }},
#define PORTSPEED		2
{PORTSPEED,  ASN_INTEGER,  RWRITE,  var_portAttributeTable, 3,  { 1 , 1, 2 }},
#define PORTDUPLEX		3
{PORTDUPLEX,  ASN_INTEGER,  RWRITE,  var_portAttributeTable, 3,  { 1 , 1, 3 }},
#define PORTVLANMODE		4
{PORTVLANMODE,  ASN_INTEGER,  RWRITE,  var_portAttributeTable, 3,  { 1 , 1, 4 }},
#define PORTPROTECTED		5
{PORTPROTECTED,  ASN_INTEGER,  RWRITE,  var_portAttributeTable, 3,  { 1 , 1, 5 }},
#define PORTDESCRIPTION		6
{PORTDESCRIPTION,  ASN_OCTET_STR,  RWRITE,  var_portAttributeTable, 3,  { 1 , 1, 6 }},
#define PORTINDEX		1
{PORTINDEX,  ASN_INTEGER,  RONLY,   var_PortCtrlTable, 3,  { 2 , 1, 1 }},
#define PORTLIMITINGRESS		2
{PORTLIMITINGRESS,  ASN_INTEGER,  RWRITE,  var_PortCtrlTable, 3,  { 2 , 1, 2 }},
#define PORTLIMITEGRESS		3
{PORTLIMITEGRESS,  ASN_INTEGER,  RWRITE,  var_PortCtrlTable, 3,  { 2 , 1, 3 }},
#define PORTINDEX		1
{PORTINDEX,  ASN_INTEGER,  RONLY,   var_PortStaticVlanTable, 3,  { 3 , 1, 1 }},
#define VLANSTATICEGRESSPORT		2
{VLANSTATICEGRESSPORT,  ASN_OCTET_STR,  RWRITE,  var_PortStaticVlanTable, 3,  { 3 , 1, 2 }},
#define VLANSTATICUNTAGGEDPORT		3
{VLANSTATICUNTAGGEDPORT,  ASN_OCTET_STR,  RWRITE,  var_PortStaticVlanTable, 3,  { 3 , 1, 3 }},
#define VLANSTATICROWSTATUS		4
{VLANSTATICROWSTATUS,  ASN_INTEGER,  RWRITE,  var_PortStaticVlanTable, 3,  { 3 , 1, 8 }},
};
/*    (L = length of the oidsuffix) */


/** Initializes the sysPortCtrlMib module */
void
init_sysPortCtrlMib(void)
{

    DEBUGMSGTL(("sysPortCtrlMib", "Initializing\n"));
    
	sal_private_oid_oem_init(sysPortCtrlMib_variables_oid);

    /* register ourselves with the agent to handle our mib tree */
    REGISTER_MIB("sysPortCtrlMib", sysPortCtrlMib_variables, variable4,
               sysPortCtrlMib_variables_oid);

    /* place any other initialization junk you need here */
}

/*
 * var_sysPortCtrlMib():
 *   This function is called every time the agent gets a request for
 *   a scalar variable that might be found within your mib section
 *   registered above.  It is up to you to do the right thing and
 *   return the correct value.
 *     You should also correct the value of "var_len" if necessary.
 *
 *   Please see the documentation for more information about writing
 *   module extensions, and check out the examples in the examples
 *   and mibII directories.
 */
unsigned char *
var_sysPortCtrlMib(struct variable *vp, 
                oid     *name, 
                size_t  *length, 
                int     exact, 
                size_t  *var_len, 
                WriteMethod **write_method)
{
    /* variables we may use later */
    static long long_ret;
    static u_long ulong_ret;
    static unsigned char string[SPRINT_MAX_LEN];
    static oid objid[MAX_OID_LEN];
    static struct counter64 c64;

    if (header_generic(vp,name,length,exact,var_len,write_method)
                                  == MATCH_FAILED )
    return NULL;

    /* 
   * this is where we do the value assignments for the mib results.
   */
    switch(vp->magic) {
    default:
      ERROR_MSG("");
    }
    return NULL;
}


/*
 * var_portAttributeTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_sysPortCtrlMib above.
 */
unsigned char *
var_portAttributeTable(struct variable *vp,
    	    oid     *name,
    	    size_t  *length,
    	    int     exact,
    	    size_t  *var_len,
    	    WriteMethod **write_method)
{
    /* variables we may use later */
    static long long_ret;
    static u_long ulong_ret;
    static unsigned char string[SPRINT_MAX_LEN];
    static oid objid[MAX_OID_LEN];
    static struct counter64 c64;
	unsigned long	index;
	static unsigned char portDes[CAPA_PORTDESC_STR_LEN+1];

    /* 
   * This assumes that the table is a 'simple' table.
   *	See the implementation documentation for the meaning of this.
   *	You will need to provide the correct value for the TABLE_SIZE parameter
   *
   * If this table does not meet the requirements for a simple table,
   *	you will need to provide the replacement code yourself.
   *	Mib2c is not smart enough to write this for you.
   *    Again, see the implementation documentation for what is required.
   */
    if (header_simple_table(vp,name,length,exact,var_len,write_method, LP2UP(LOGIC_PORT_END) + 1)
                                                == MATCH_FAILED )
    return NULL;
    
	index = vp->namelen >= *length ? 0 : UP2LP(name[vp->namelen]);
	
	if (exact) /* GET packet for one instance */
	{
		if (*length != 15 )
			return NULL;
	}
	else /* Get-Next packet */
	{
		if (-1 == header_logicPort(&index, vp, name, length))
			return NULL;
	}
	
	if (IS_LOGIC_PORT_INVALID(index))
		return NULL;

	*var_len = sizeof(long);

    /* 
   * this is where we do the value assignments for the mib results.
   */
    switch(vp->magic) {
    case PORTINDEX:
        ulong_ret = LP2UP(index);	/* XXX */
        return (u_char*) &ulong_ret;
    case PORTSPEED:
        *write_method = write_portSpeed;
        sal_mib_port_Attribute_get(index, PORTSPEED, &ulong_ret);
        return (u_char*) &ulong_ret;
    case PORTDUPLEX:
        *write_method = write_portDuplex;
        sal_mib_port_Attribute_get(index, PORTDUPLEX, &ulong_ret);
        return (u_char*) &ulong_ret;
    case PORTVLANMODE:
        *write_method = write_portVlanMode;
        sal_mib_port_ctrl_get(index, PORTVLANMODE, &ulong_ret);
        return (u_char*) &ulong_ret;
    case PORTPROTECTED:
        *write_method = write_portProtected;
        sal_mib_port_ctrl_get(index, PORTPROTECTED, &ulong_ret);
        return (u_char*) &ulong_ret;
    case PORTDESCRIPTION:
        *write_method = write_portDescription;
        //memset(portDes, 0, sizeof(portDes));
        sal_port_descp_get(index, portDes);
        *var_len = strlen(portDes);
        return (u_char*) &portDes;
    default:
      ERROR_MSG("");
    }
    return NULL;
}
/*
 * var_PortCtrlTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_sysPortCtrlMib above.
 */
unsigned char *
var_PortCtrlTable(struct variable *vp,
    	    oid     *name,
    	    size_t  *length,
    	    int     exact,
    	    size_t  *var_len,
    	    WriteMethod **write_method)
{
    /* variables we may use later */
    static long long_ret;
    static u_long ulong_ret;
    static unsigned char string[SPRINT_MAX_LEN];
    static oid objid[MAX_OID_LEN];
    static struct counter64 c64;
	unsigned long	index;

    /* 
   * This assumes that the table is a 'simple' table.
   *	See the implementation documentation for the meaning of this.
   *	You will need to provide the correct value for the TABLE_SIZE parameter
   *
   * If this table does not meet the requirements for a simple table,
   *	you will need to provide the replacement code yourself.
   *	Mib2c is not smart enough to write this for you.
   *    Again, see the implementation documentation for what is required.
   */
    if (header_simple_table(vp,name,length,exact,var_len,write_method, LP2UP(NORMAL_LOGIC_PORT_END))
                                                == MATCH_FAILED )
    return NULL;
    
	index = vp->namelen >= *length ? 0 : UP2LP(name[vp->namelen]);
		
	if (exact) /* GET packet for one instance */
	{
		if (*length != 15 )
			return NULL;
	}
	else /* Get-Next packet */
	{
		if (-1 == header_logicPort(&index, vp, name, length))
			return NULL;
	}

	if (IS_LOGIC_PORT_INVALID(index))
		return NULL;

	*var_len = sizeof(long);

    /* 
   * this is where we do the value assignments for the mib results.
   */
    switch(vp->magic) {
    case PORTINDEX:
        ulong_ret = LP2UP(index);	/* XXX */
        return (u_char*) &ulong_ret;
    case PORTLIMITINGRESS:
        *write_method = write_portLimitIngress;
        sal_mib_port_ctrl_get(index, PORTLIMITINGRESS, &ulong_ret);
        return (u_char*) &ulong_ret;
    case PORTLIMITEGRESS:
        *write_method = write_portLimitegress;
        sal_mib_port_ctrl_get(index, PORTLIMITEGRESS, &ulong_ret);
        return (u_char*) &ulong_ret;
    default:
      ERROR_MSG("");
    }
    return NULL;
}
/*
 * var_PortStaticVlanTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_sysPortCtrlMib above.
 */
unsigned char *
var_PortStaticVlanTable(struct variable *vp,
    	    oid     *name,
    	    size_t  *length,
    	    int     exact,
    	    size_t  *var_len,
    	    WriteMethod **write_method)
{
    /* variables we may use later */
    static long long_ret;
    static u_long ulong_ret;
    static unsigned char string[256];
    static oid objid[MAX_OID_LEN];
    static struct counter64 c64;
	unsigned long	index;

    /* 
   * This assumes that the table is a 'simple' table.
   *	See the implementation documentation for the meaning of this.
   *	You will need to provide the correct value for the TABLE_SIZE parameter
   *
   * If this table does not meet the requirements for a simple table,
   *	you will need to provide the replacement code yourself.
   *	Mib2c is not smart enough to write this for you.
   *    Again, see the implementation documentation for what is required.
   */
    if (header_simple_table(vp,name,length,exact,var_len,write_method, LP2UP(LOGIC_PORT_END) + 1)
                                                == MATCH_FAILED )
    return NULL;

	index = vp->namelen >= *length ? 0 : UP2LP(name[vp->namelen]);
	
	if (exact) /* GET packet for one instance */
	{
		if (*length != 15 )
			return NULL;
	}
	else /* Get-Next packet */
	{
		if (-1 == header_logicPort(&index, vp, name, length))
			return NULL;
	}

	if (IS_LOGIC_PORT_INVALID(index))
		return NULL;

	*var_len = sizeof(long);

    /* 
   * this is where we do the value assignments for the mib results.
   */
    switch(vp->magic) {
    case PORTINDEX:
        ulong_ret = LP2UP(index);	/* XXX */
        return (u_char*) &ulong_ret;
    case VLANSTATICEGRESSPORT:
        *write_method = write_vlanStaticEgressPort;
        osal_memset(string, 0, sizeof(string));
        sal_mib_port_static_vlan_get(VLANSTATICEGRESSPORT, index, string);
        *var_len = osal_strlen(string);
        return (u_char*) &string;
    case VLANSTATICUNTAGGEDPORT:
        *write_method = write_vlanStaticUntaggedPort;
        osal_memset(string, 0, sizeof(string));
        sal_mib_port_static_vlan_get(VLANSTATICUNTAGGEDPORT, index, string);
        *var_len = osal_strlen(string);
        return (u_char*) &string;
    case VLANSTATICROWSTATUS:
        *write_method = write_vlanStaticRowStatus;
        ulong_ret = 0;
        return (u_char*) &ulong_ret;
    default:
      ERROR_MSG("");
    }
    return NULL;
}


int
write_portSpeed(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;
    ulong index;
    int ret = SYS_ERR_OK;

    index = UP2LP(name[name_len-1]);

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to sysPortCtrlMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to sysPortCtrlMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          ret = sal_mib_port_Attribute_set(index, PORTSPEED, value);
		  if(ret != SYS_ERR_OK)
		  {
		      return SNMP_ERR_WRONGVALUE;
		  }
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_portDuplex(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;
    ulong index;

    index = UP2LP(name[name_len-1]);

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to sysPortCtrlMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to sysPortCtrlMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          /*10G 不允许配置半双工模式*/
          if(LP2TYPE(index) == PORT_TYPE_10G && (value == 2))
          {
		  	return SNMP_ERR_WRONGVALUE;
          }
          if((index >= TRUNK_LOGIC_PORT_START) && (value == 2))
          	 return SNMP_ERR_WRONGVALUE;
		  SYS_ERR_CHK(sal_mib_port_Attribute_set(index, PORTDUPLEX, value));
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_portVlanMode(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;
    ulong index;

    index = UP2LP(name[name_len-1]);

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to sysPortCtrlMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to sysPortCtrlMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
		  SYS_ERR_CHK(sal_mib_port_ctrl_set(index, PORTVLANMODE, value));
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_portProtected(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;
    ulong index;

    index = UP2LP(name[name_len-1]);

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to sysPortCtrlMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to sysPortCtrlMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if(0 < value && value <= 2)
          {
              SYS_ERR_CHK(sal_mib_port_ctrl_set(index, PORTPROTECTED, value));
          }
          else 
          {
				return SNMP_ERR_WRONGVALUE;
          }
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_portDescription(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    char value[CAPA_PORTDESC_STR_LEN+1];
    int size;
    ulong index;
    int rc = 0;

    index = UP2LP(name[name_len-1]);

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_OCTET_STR) {
              fprintf(stderr, "write to sysPortCtrlMib not ASN_OCTET_STR\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(value)-1) {
              fprintf(stderr,"write to sysPortCtrlMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          osal_memset(value, 0, sizeof(value));
          osal_memcpy(value, var_val, size);
		  rc = sal_port_descp_set(index, value);
		  if(rc != 0)
		  {
		  	return SNMP_ERR_WRONGVALUE;
		  }
		  return rc;
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}

int
write_portLimitIngress(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;
    ulong index;

    index = UP2LP(name[name_len-1]);

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to sysPortCtrlMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to sysPortCtrlMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if(16 <= value && value <= 1000000 || value == 0)
          {
              SYS_ERR_CHK(sal_mib_port_ctrl_set(index, PORTLIMITINGRESS, value));
          }
          else 
          {
				return SNMP_ERR_WRONGVALUE;
          }

          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_portLimitegress(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;
    ulong index;

    index = UP2LP(name[name_len-1]);

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to sysPortCtrlMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to sysPortCtrlMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if(16 <= value && value <= 1000000 || value == 0)
          {
              SYS_ERR_CHK(sal_mib_port_ctrl_set(index, PORTLIMITEGRESS, value));
          }
          else
          {
			  return SNMP_ERR_WRONGVALUE;
          }
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_vlanStaticEgressPort(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    char value[256];
    int size;
    uint32 port;
    uint32 rc = 0;

    port = UP2LP(name[name_len-1]);

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_OCTET_STR) {
              fprintf(stderr, "write to sysPortCtrlMib not ASN_OCTET_STR\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(value)) {
              fprintf(stderr,"write to sysPortCtrlMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          //value = * (char *) var_val;
          osal_memset(value, 0, sizeof(value));
          osal_memcpy(value, var_val, size);
          if( osal_strcmp(value, "")  != 0 )
          {
		      rc = sal_mib_port_static_vlan_set(VLANSTATICEGRESSPORT, port, value, TRUE);
		  }
		  else
		  {
		  	  return SNMP_ERR_NOACCESS;
		  }
		  if(rc != SYS_ERR_OK)
		  	 return SNMP_ERR_WRONGVALUE;
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_vlanStaticUntaggedPort(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    char value[256];
    int size;
    uint32 port;
	uint32 rc = 0;
    port = UP2LP(name[name_len-1]);

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_OCTET_STR) {
              fprintf(stderr, "write to sysPortCtrlMib not ASN_OCTET_STR\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(value)) {
              fprintf(stderr,"write to sysPortCtrlMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          osal_memset(value, 0, sizeof(value));
          osal_memcpy(value, var_val, size);
          if( osal_strcmp(value, "")  != 0 )
          {
		      rc = sal_mib_port_static_vlan_set(VLANSTATICUNTAGGEDPORT, port, value, TRUE);
		  }
		  else
		  {
		  	  return SNMP_ERR_NOACCESS;
		  }

		  if(rc != SYS_ERR_OK)
		  	  return SNMP_ERR_WRONGVALUE;
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_vlanStaticRowStatus(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;
    uint32 port;

    port = UP2LP(name[name_len-1]);

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to sysPortCtrlMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to sysPortCtrlMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if(value == 1)
		  {
		  	sal_mib_port_static_vlan_set(VLANSTATICROWSTATUS, port, NULL, TRUE);
		  }
		  else if(value == 2)
		  {
			sal_mib_port_static_vlan_set(VLANSTATICROWSTATUS, port, NULL, FALSE);
		  }
		  else
		  	return SNMP_ERR_WRONGVALUE;
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
