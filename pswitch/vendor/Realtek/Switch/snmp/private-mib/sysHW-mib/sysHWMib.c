/*
 * Note: this file originally auto-generated by mib2c using
 *        : mib2c.old-api.conf 17851 2009-11-30 16:46:06Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include <common/sys_def.h>
#include <libsal/sal_mib_private.h>
#include "sysHWMib.h"


/* 
 * sysHWMib_variables_oid:
 *   this is the top level oid that we want to register under.  This
 *   is essentially a prefix, with the suffix appearing in the
 *   variable below.
 */

oid sysHWMib_variables_oid[] = { 1,3,6,1,4,1,27282,1,1,1,1 };

/* 
 * variable4 sysHWMib_variables:
 *   this variable defines function callbacks and type return information 
 *   for the sysHWMib mib section 
 */

struct variable4 sysHWMib_variables[] = {
/*  magic number        , variable type , ro/rw , callback fn  , L, oidsuffix */
#define MEMORYTOTAL		1
{MEMORYTOTAL,  ASN_INTEGER,  RONLY ,  var_sysHWMib, 2,  { 1,1 }},
#define MEMORYUSED		2
{MEMORYUSED,  ASN_INTEGER,  RONLY ,  var_sysHWMib, 2,  { 1,2 }},
#define MEMORYFREE		3
{MEMORYFREE,  ASN_INTEGER,  RONLY ,  var_sysHWMib, 2,  { 1,3 }},
#define CPUNAME		4
{CPUNAME,  ASN_OCTET_STR,  RONLY ,  var_sysHWMib, 2,  { 2,1 }},
#define CPUUSED		5
{CPUUSED,  ASN_INTEGER,  RONLY ,  var_sysHWMib, 2,  { 2,2 }},
#define CPUTEMPERATURE		6
{CPUTEMPERATURE,  ASN_INTEGER,  RONLY ,  var_sysHWMib, 2,  { 2,3 }},
#define SYSPOEFUNCTION		7
{SYSPOEFUNCTION,  ASN_INTEGER,  RONLY ,  var_sysHWMib, 1,  { 4 }},

#define INTERFACEINDEX		1
{INTERFACEINDEX,  ASN_INTEGER,  RONLY,   var_InterfaceTypeInfoTable, 3,  { 3 , 1, 1 }},
#define PORTTYPE		2
{PORTTYPE,  ASN_INTEGER,  RONLY,   var_InterfaceTypeInfoTable, 3,  { 3 , 1, 2 }},
};
/*    (L = length of the oidsuffix) */


/** Initializes the sysHWMib module */
void
init_sysHWMib(void)
{

    DEBUGMSGTL(("sysHWMib", "Initializing\n"));

	sal_private_oid_oem_init(sysHWMib_variables_oid);

    /* register ourselves with the agent to handle our mib tree */
    REGISTER_MIB("sysHWMib", sysHWMib_variables, variable4,
               sysHWMib_variables_oid);

    /* place any other initialization junk you need here */
}

/*
 * var_sysHWMib():
 *   This function is called every time the agent gets a request for
 *   a scalar variable that might be found within your mib section
 *   registered above.  It is up to you to do the right thing and
 *   return the correct value.
 *     You should also correct the value of "var_len" if necessary.
 *
 *   Please see the documentation for more information about writing
 *   module extensions, and check out the examples in the examples
 *   and mibII directories.
 */
unsigned char *
var_sysHWMib(struct variable *vp, 
                oid     *name, 
                size_t  *length, 
                int     exact, 
                size_t  *var_len, 
                WriteMethod **write_method)
{
    /* variables we may use later */
    static long long_ret;
    static u_long ulong_ret;
    static unsigned char string[SPRINT_MAX_LEN];
    static oid objid[MAX_OID_LEN];
    static struct counter64 c64;

    if (header_generic(vp,name,length,exact,var_len,write_method)
                                  == MATCH_FAILED )
    return NULL;

    /* 
   * this is where we do the value assignments for the mib results.
   */
    switch(vp->magic) {
    case MEMORYTOTAL:
        sal_mib_memory_info_get(MEMORYTOTAL, &ulong_ret);
        return (u_char*) &ulong_ret;
    case MEMORYUSED:
        sal_mib_memory_info_get(MEMORYUSED, &ulong_ret);
        return (u_char*) &ulong_ret;
    case MEMORYFREE:
		sal_mib_memory_info_get(MEMORYFREE, &ulong_ret);
        return (u_char*) &ulong_ret;
    case CPUNAME:
    	sal_mib_cpu_info_get(CPUNAME, (void*)&string);
        return (u_char*) &string;
    case CPUUSED:
    	sal_mib_cpu_info_get(CPUUSED, (void*)&ulong_ret);
        return (u_char*) &ulong_ret;
    case CPUTEMPERATURE:
    	sal_mib_cpu_info_get(CPUTEMPERATURE, (void*)&ulong_ret);
        return (u_char*) &ulong_ret;
    case SYSPOEFUNCTION:
        sal_mib_poe_func_get(&ulong_ret);	/* XXX */
        return (u_char*) &ulong_ret;
    default:
      ERROR_MSG("");
    }
    return NULL;
}


/*
 * var_InterfaceTypeInfoTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_sysHWMib above.
 */
unsigned char *
var_InterfaceTypeInfoTable(struct variable *vp,
    	    oid     *name,
    	    size_t  *length,
    	    int     exact,
    	    size_t  *var_len,
    	    WriteMethod **write_method)
{
    /* variables we may use later */
    static long long_ret;
    static u_long ulong_ret;
    static unsigned char string[SPRINT_MAX_LEN];
    static oid objid[MAX_OID_LEN];
    static struct counter64 c64;
	unsigned long	index;

    /* 
   * This assumes that the table is a 'simple' table.
   *	See the implementation documentation for the meaning of this.
   *	You will need to provide the correct value for the TABLE_SIZE parameter
   *
   * If this table does not meet the requirements for a simple table,
   *	you will need to provide the replacement code yourself.
   *	Mib2c is not smart enough to write this for you.
   *    Again, see the implementation documentation for what is required.
   */
    if (header_simple_table(vp,name,length,exact,var_len,write_method, LP2UP(NORMAL_LOGIC_PORT_END))
                                                == MATCH_FAILED )
    return NULL;
    
    index = vp->namelen >= *length ? 0 : UP2LP(name[vp->namelen]);

    if (exact) /* GET packet for one instance */
    {
        if (*length != 15 )
            return NULL;
    }
    else /* Get-Next packet */
    {
        if (-1 == header_logicPort(&index, vp, name, length))
            return NULL;
    }

    if (IS_LOGIC_PORT_INVALID(index))
        return NULL;

    *var_len = sizeof(long);

    /* 
   * this is where we do the value assignments for the mib results.
   */
    switch(vp->magic) {
    case INTERFACEINDEX:
        ulong_ret = LP2UP(index);	/* XXX */
        return (u_char*) &ulong_ret;
    case PORTTYPE:
    	/*1：表示copper 2：表示 fiber      3：表示堆叠*/
        sal_mib_interface_type_info_get(index, &ulong_ret);
        return (u_char*) &ulong_ret;
    default:
      ERROR_MSG("");
    }
    return NULL;
}


