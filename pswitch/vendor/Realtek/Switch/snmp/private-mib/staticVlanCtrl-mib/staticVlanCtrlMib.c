/*
 * Note: this file originally auto-generated by mib2c using
 *        : mib2c.old-api.conf 17851 2009-11-30 16:46:06Z dts12 $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include <common/sys_def.h>
#include <libsal/sal_mib_private.h>
#include <libsal/sal_vlan.h>

#include "staticVlanCtrlMib.h"

/* 
 * staticVlanCtrlMib_variables_oid:
 *   this is the top level oid that we want to register under.  This
 *   is essentially a prefix, with the suffix appearing in the
 *   variable below.
 */

oid staticVlanCtrlMib_variables_oid[] = { 1,3,6,1,4,1,27282,1,1,1,8 };

/* 
 * variable4 staticVlanCtrlMib_variables:
 *   this variable defines function callbacks and type return information 
 *   for the staticVlanCtrlMib mib section 
 */

struct variable4 staticVlanCtrlMib_variables[] = {
/*  magic number        , variable type , ro/rw , callback fn  , L, oidsuffix */

#define STATICVLANNAME		1
{STATICVLANNAME,  ASN_OCTET_STR,  RWRITE,  var_staticVlanTable, 4,  { 1,1 , 1, 1 }},
#define STATICVLANEGRESSPORTS		2
{STATICVLANEGRESSPORTS,  ASN_OCTET_STR,  RWRITE,  var_staticVlanTable, 4,  { 1,1 , 1, 2 }},
#define STATICVLANUNTAGPORTS		3
{STATICVLANUNTAGPORTS,  ASN_OCTET_STR,  RWRITE,  var_staticVlanTable, 4,  { 1,1 , 1, 3 }},
#define STATICVLANROWSTATUS		4
{STATICVLANROWSTATUS,  ASN_INTEGER,  RWRITE,  var_staticVlanTable, 4,  { 1,1 , 1, 4 }},
};
/*    (L = length of the oidsuffix) */


/** Initializes the staticVlanCtrlMib module */
void
init_staticVlanCtrlMib(void)
{

    DEBUGMSGTL(("staticVlanCtrlMib", "Initializing\n"));

	sal_private_oid_oem_init(staticVlanCtrlMib_variables_oid);

    /* register ourselves with the agent to handle our mib tree */
    REGISTER_MIB("staticVlanCtrlMib", staticVlanCtrlMib_variables, variable4,
               staticVlanCtrlMib_variables_oid);

    /* place any other initialization junk you need here */
}

/*
 * var_staticVlanCtrlMib():
 *   This function is called every time the agent gets a request for
 *   a scalar variable that might be found within your mib section
 *   registered above.  It is up to you to do the right thing and
 *   return the correct value.
 *     You should also correct the value of "var_len" if necessary.
 *
 *   Please see the documentation for more information about writing
 *   module extensions, and check out the examples in the examples
 *   and mibII directories.
 */
unsigned char *
var_staticVlanCtrlMib(struct variable *vp, 
                oid     *name, 
                size_t  *length, 
                int     exact, 
                size_t  *var_len, 
                WriteMethod **write_method)
{
    /* variables we may use later */
    static long long_ret;
    static u_long ulong_ret;
    static unsigned char string[SPRINT_MAX_LEN];
    static oid objid[MAX_OID_LEN];
    static struct counter64 c64;

    if (header_generic(vp,name,length,exact,var_len,write_method)
                                  == MATCH_FAILED )
    return NULL;

    /* 
   * this is where we do the value assignments for the mib results.
   */
    switch(vp->magic) {
    default:
      ERROR_MSG("");
    }
    return NULL;
}


/*
 * var_staticVlanTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_staticVlanCtrlMib above.
 */
unsigned char *
var_staticVlanTable(struct variable *vp,
    	    oid     *name,
    	    size_t  *length,
    	    int     exact,
    	    size_t  *var_len,
    	    WriteMethod **write_method)
{
    /* variables we may use later */
    static long long_ret;
    static u_long ulong_ret;
    static unsigned char string[SYS_USER_PORTMASK_LEN+1];
    static oid objid[MAX_OID_LEN];
    static struct counter64 c64;
	unsigned long	vlanIndex;

    /* 
   * This assumes that the table is a 'simple' table.
   *	See the implementation documentation for the meaning of this.
   *	You will need to provide the correct value for the TABLE_SIZE parameter
   *
   * If this table does not meet the requirements for a simple table,
   *	you will need to provide the replacement code yourself.
   *	Mib2c is not smart enough to write this for you.
   *    Again, see the implementation documentation for what is required.
   */
    if (header_simple_table(vp,name,length,exact,var_len,write_method, CAPA_VLAN_NUM)
                                                == MATCH_FAILED )
    return NULL;
    
	//SNMP_DBG("length=%d namelen=%d", *length, vp->namelen);
	vlanIndex = vp->namelen >= *length ? 0 : name[vp->namelen];
	if (exact) /* GET packet for one instance */
	{
		if (*length != 16 )
			return NULL;
	}
	else /* Get-Next packet */
	{	
		if(vlanIndex == 1)
		{
			SNMP_DBG();
			sal_mib_static_vlan_get();
		}
	}

	*var_len = sizeof(long);

    /* 
   * this is where we do the value assignments for the mib results.
   */
    switch(vp->magic) {
    case STATICVLANNAME:
        *write_method = write_staticvlanName;
        osal_memset(string, 0, sizeof(string));
        if(exact)
        {
			sal_mib_static_vlan_by_index_get(STATICVLANNAME, &vlanIndex, string);
        }
        else
        {
        	sal_mib_static_vlan_by_index_getnet(STATICVLANNAME, &vlanIndex, string);
        }
        if(vlanIndex > CAPA_VLAN_NUM)
        	return NULL;
        *var_len = osal_strlen(string);
        name[vp->namelen] = vlanIndex;
		*length = vp->namelen + 1;
        return (u_char*) &(string);
    case STATICVLANEGRESSPORTS:
        *write_method = write_staticVlanEgressPorts;
        osal_memset(string, 0, sizeof(string));
        if(exact)
        {
			sal_mib_static_vlan_by_index_get(STATICVLANEGRESSPORTS, &vlanIndex, string);
        }
        else
        {
        	sal_mib_static_vlan_by_index_getnet(STATICVLANEGRESSPORTS, &vlanIndex, string);
        }
        if(vlanIndex > CAPA_VLAN_NUM)
        	return NULL;
		*var_len = sizeof(sys_logic_portmask_t);
		 name[vp->namelen] = vlanIndex;
		*length = vp->namelen + 1;
		return (u_char*) &(string);
    case STATICVLANUNTAGPORTS:
        *write_method = write_staticVlanUntagPorts;
        osal_memset(string, 0, sizeof(string));
        if(exact)
        {
			sal_mib_static_vlan_by_index_get(STATICVLANUNTAGPORTS, &vlanIndex, string);
        }
        else
        {
        	sal_mib_static_vlan_by_index_getnet(STATICVLANUNTAGPORTS, &vlanIndex, string);
        }
        if(vlanIndex > CAPA_VLAN_NUM)
        	return NULL;
		*var_len = sizeof(sys_logic_portmask_t);
		name[vp->namelen] = vlanIndex;
		*length = vp->namelen + 1;
		return (u_char*) &(string);
    case STATICVLANROWSTATUS:
        *write_method = write_staticvlanRowStatus;
        if(vlanIndex > CAPA_VLAN_NUM)
        	return NULL;
		//name[vp->namelen] = vlanIndex;
		//*length = vp->namelen + 1;
        ulong_ret = 1;	/* XXX */
        return (u_char*) &ulong_ret;
    default:
      ERROR_MSG("");
    }
    return NULL;
}


int
write_staticvlanName(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    char value[124];
    int size;
	uint32 vlanIndex;
	vlanIndex = name[name_len-1];

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_OCTET_STR) {
              fprintf(stderr, "write to staticVlanCtrlMib not ASN_OCTET_STR\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(value)) {
              fprintf(stderr,"write to staticVlanCtrlMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          osal_memset(value, 0, sizeof(value));
          osal_memcpy(value, var_val, size);
          if(osal_strcmp(value, "0") != 0)
          {
		  	sal_mib_static_vlan_create(STATICVLANNAME, vlanIndex, value, TRUE);
		  }
		  else
		  {
		  	return SNMP_ERR_WRONGVALUE;
		  }

          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_staticVlanEgressPorts(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    char value[124];
    int size;

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_OCTET_STR) {
              fprintf(stderr, "write to staticVlanCtrlMib not ASN_OCTET_STR\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(value)) {
              fprintf(stderr,"write to staticVlanCtrlMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;

          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_staticVlanUntagPorts(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    char value[124];
    int size;

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_OCTET_STR) {
              fprintf(stderr, "write to staticVlanCtrlMib not ASN_OCTET_STR\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(value)) {
              fprintf(stderr,"write to staticVlanCtrlMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;

          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
int
write_staticvlanRowStatus(int      action,
            u_char   *var_val,
            u_char   var_val_type,
            size_t   var_val_len,
            u_char   *statP,
            oid      *name,
            size_t   name_len)
{
    long value;
    int size;
	uint32 vlanIndex;
	vlanIndex = name[name_len-1];
	int rc = SYS_ERR_OK;

    switch ( action ) {
        case RESERVE1:
          if (var_val_type != ASN_INTEGER) {
              fprintf(stderr, "write to staticVlanCtrlMib not ASN_INTEGER\n");
              return SNMP_ERR_WRONGTYPE;
          }
          if (var_val_len > sizeof(long)) {
              fprintf(stderr,"write to staticVlanCtrlMib: bad length\n");
              return SNMP_ERR_WRONGLENGTH;
          }
          break;

        case RESERVE2:
          size  = var_val_len;
          value = * (long *) var_val;
          if( value < 0 && value > 6)	
			 	return SNMP_ERR_WRONGVALUE;	
		  if(value == 4)//add
		  {
			rc = sal_mib_static_vlan_create(STATICVLANROWSTATUS, vlanIndex, NULL, TRUE);
		  }
		  else if(value == 6)//remove
		  {
		  	rc = sal_mib_static_vlan_create(STATICVLANROWSTATUS, vlanIndex, NULL, FALSE);
		  }

		  if(rc != SYS_ERR_OK)
		  	return SNMP_ERR_WRONGVALUE;
          break;

        case FREE:
             /* Release any resources that have been allocated */
          break;

        case ACTION:
             /*
              * The variable has been stored in 'value' for you to use,
              * and you have just been asked to do something with it.
              * Note that anything done here must be reversable in the UNDO case
              */
          break;

        case UNDO:
             /* Back out any changes made in the ACTION case */
          break;

        case COMMIT:
             /*
              * Things are working well, so it's now safe to make the change
              * permanently.  Make sure that anything done here can't fail!
              */
          break;
    }
    return SNMP_ERR_NOERROR;
}
